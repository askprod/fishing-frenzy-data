<% if @pets.any? %>
	<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
		<% @pets.each do |pet| %>
      <%= render UI::ItemCardComponent.new(**item_card_pet_attributes(pet)) %>
		<% end %>
	</div>
<% else %>
    <p class="italic text-gray-300 text-center text-xs">No data here...</p>
<% end %>
